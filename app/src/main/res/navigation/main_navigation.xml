<?xml version="1.0" encoding="utf-8"?>
<navigation xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/main_navigation"
    app:startDestination="@id/fragment_search_movie">

    <fragment
        android:id="@+id/fragment_search_movie"
        android:name="com.utad.firststeps.ui.movies.search.MovieSearchFragment"
        android:label="@string/search"
        tools:layout="@layout/fragment_search_movie" />
    <fragment
        android:id="@+id/fragment_favorites"
        android:name="com.utad.firststeps.ui.movies.favorites.FavoritesFragment"
        android:label="@string/favorites"
        tools:layout="@layout/fragment_favorites" />
    <fragment
        android:id="@+id/fragment_profile"
        android:name="com.utad.firststeps.ui.profile.ProfileFragment"
        android:label="@string/profile"
        tools:layout="@layout/fragment_profile" />
</navigation>